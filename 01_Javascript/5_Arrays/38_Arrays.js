//Examples of Arrays

let fruits = ["Apple","Banana","Pineapple","Guava"]
let numArray = [1,3,5,7,23,345,7]
let browsers = ["chrome", "firefox", "safari"]
let score = new Array(3)
let score2 = new Array(1,2,3)
let score3 = Array.of(10,20,30,40)
let score4 = Array.from("12345")


score[1]=1
console.log(score4)


// - The **index** is the row number (zero-based: the first row is `0` ).
// - **Adding/removing** rows is `push` /`pop`  (at the end) or `shift` /`unshift`  (at the start) — like appending a new test case or pulling the next one off a queue.Searching (includes, indexOf, find) is scanning the sheet for a value.
// - **Searching** (`includes` , `indexOf` , `find` ) is scanning the sheet for a value.
// - **Transforming** (`map` , `filter` , `reduce` ) is producing a new sheet from the old one without touching the original — which keeps your test data immutable and predictable.
